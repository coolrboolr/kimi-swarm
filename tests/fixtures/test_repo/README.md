This directory contains a lightweight sample repository used for manual/live testing.

Notes:
- Do not check in runtime artifacts (e.g. `.ambient/`, `__pycache__/`, `*.pyc`).
- Do not check in a nested `.git/` directory. Scripts should `git init` a temporary copy.

